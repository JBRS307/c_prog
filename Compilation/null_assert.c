#include <stdio.h>
#include <stdlib.h>

double arr_avg(int* arr, size_t n) {
    assert(arr != NULL && "Pointer arr cannot be null");
    // TODO
}

int main() {
    // TODO

    return EXIT_SUCCESS;
}