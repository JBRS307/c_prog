#include <stdio.h>
#include <stdlib.h>

#define concat(a, b) a ## b
#define to_str(str) #str

int main() {
    int number = 25;
    printf("Concatenated values: %d\n", concat(num, ber));
    printf(to_str(Random text here));
    putchar('\n');

    return EXIT_SUCCESS;
}