#include <stdio.h>
#include <stdlib.h>

int max(const int* arr, size_t n) {
    
}

int main() {
    size_t n;
    printf("Enter array size: ");
    scanf("%lu", &n);

    int* arr = (int*)malloc(n * sizeof(int));



    return EXIT_SUCCESS;
}